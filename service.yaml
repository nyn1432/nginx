apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  type: Nodeport
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80         # External port
      targetPort: 80   # Container port
